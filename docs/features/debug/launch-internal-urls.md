# 启动微信内部 URL

> 跳转微信 weixin:// URL

## 类别

调试

## 类型

点击使用

## 描述

允许输入并跳转微信内部 `weixin://` 协议 URL。微信内部通过此类 URL 打开特定页面、启动特定功能或触发特定行为, 本功能可直接在输入框中输入任意 `weixin://` URL 并执行跳转, 方便开发者测试和探索微信内部功能。

## 使用方法

在模块设置中点击输入要跳转的 weixin:// URL 并确定
