# @所有人

入口：WeKit 设置 → 功能 → 聊天 → @所有人。

开启后，在群聊输入框填写消息，长按发送按钮或加号按钮打开菜单，选择「@所有人」，再选择要提醒的成员。

长按菜单中的该项可配置「隐蔽@所有人」，默认关闭。关闭时在文本前附加 @昵称；开启时发送输入框文本并携带提醒成员信息，不显示 @昵称前缀。单次最多提醒前 200 名成员。

只能在群聊中使用，需要非空消息和成员选择。不同发送模式下自己看到的记录可能不同，请按实际发送结果确认。
