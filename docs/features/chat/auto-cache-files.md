# 自动缓存文件

入口：WeKit 设置 → 功能 → 聊天 → 自动缓存文件。

开启后监听收到的文件消息，在对方上传完成后触发微信内部下载，将文件保存在微信缓存中。

点击功能选择黑名单或白名单：默认使用黑名单且名单为空；黑名单内的联系人跳过缓存，白名单模式只缓存选中联系人的文件。自动缓存会使用网络和存储空间。

需要把文件导出到本地下载目录时，使用[文件下载到本地](download-files-to-local-storage.md)。
