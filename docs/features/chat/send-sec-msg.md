# 安全消息发送

入口：WeKit 设置 → 功能 → 聊天 → 安全消息发送。

开启后给自己发出的文本消息附加安全消息属性，使支持此属性的客户端限制长按操作。

点击功能选择模式：

- 被动模式：默认模式，对所有自己发出的文本消息生效。
- 主动模式（菜单）：长按发送按钮，选择「发送安全消息」，仅发送当前输入框内容。
- 主动模式（触发前缀）：默认前缀 `#sec`，只有以此前缀开头的消息生效，发送时去掉前缀。

这不是加密、禁止截图或防止复制的可靠保护。[反安全消息](anti-sec-msg.md)等客户端功能可以忽略该属性。
