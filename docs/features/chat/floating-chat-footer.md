# 悬浮输入框

入口：WeKit 设置 → 功能 → 聊天 → 悬浮输入框。

将聊天输入区域显示为带圆角、侧边距和阴影的悬浮卡片。开启后点击功能调整：

| 设置 | 默认值 |
| --- | --- |
| 菜单显示在输入框上方 | 开启 |
| 圆角半径 | 24 dp |
| 侧边距 | 12 dp |
| 底部间距 | 8 dp |
| 阴影强度 | 4 dp |

上方菜单模式让表情和工具菜单从输入框上沿展开；关闭该项则维持微信原有菜单位置。
