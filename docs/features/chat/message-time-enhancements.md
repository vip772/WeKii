# 消息时间增强

入口：WeKit 设置 → 功能 → 聊天 → 消息时间增强。

开启后显示更精确的消息时间，并可组合消息元数据。点击功能设置显示模板、时间格式、文字大小、亮/暗色、居中显示和是否显示每条消息时间。

模板支持 `$time`、`$relativeTime`、`$type`、`$msgId`、`$msgSvrId`、`$mentionedUsers`，可直接点击设置页的占位符插入。默认模板 `$time | $type`，时间格式 `yyyy/MM/dd HH:mm:ss`，文字大小 11；居中与强制每条显示默认关闭。
