# Markdown 渲染

入口：WeKit 设置 → 功能 → 聊天 → Markdown 渲染。

开启后渲染聊天中的 Markdown 文本。点击功能选择解析与渲染引擎：微信原生、markdown-rs + Html 或 Markwon。

可选择禁止标题等内容改变字体大小；markdown-rs + Html 引擎还可选择紧凑 HTML 渲染，以减少段落空行。不同引擎支持的语法和布局有所不同，微信原生路径还依赖宿主提供对应能力。
