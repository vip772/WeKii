# 自定义消息气泡

入口：WeKit 设置 → 功能 → 界面美化 / 聊天 → 自定义消息气泡。

开启后点击功能，分别设置自己与对方消息的文字色、背景色以及 `.9.png` 气泡图片；亮色和暗色模式可以独立配置。

通过设置中的「导入/更换」选择九宫格 PNG，模块会验证格式与尺寸，也可以删除已导入的图片。不要把普通大尺寸照片作为气泡文件导入。

颜色和图片只影响本地聊天显示，实际覆盖取决于对应消息类型的绘制方式。
