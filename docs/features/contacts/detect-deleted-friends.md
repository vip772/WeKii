# 检测单向删除好友

入口：WeKit 设置 → 功能 → 联系人与群组 → 检测单向删除好友。

点击功能后选择检测方式和请求间隔，确认提示后扫描好友，无需开启开关。

可选转账预检查发包或验证用户关系发包，默认转账预检查、间隔 2 秒。结果区分转账响应异常、被删除、被拉黑和账号受限等状态；响应异常不能直接认定为单向删除。

可以终止扫描并查看已得到的结果，复制信息、打标签，或确认后删除结果中的好友。服务器提示操作频繁时会停止，请稍后重试。删除好友是实际操作，应先核对结果。
