# 转发 & 一键转发

入口：WeKit 设置 → 功能 → 朋友圈 → 转发 & 一键转发。

开启后，长按朋友圈选择「转发」或「一键转发」。转发打开编辑页面，由你确认发布；一键转发直接尝试发布内容。

支持文字、图片、视频及实况图片等当前实现支持的类型。普通图片和视频会在转发前自动缓存，无需先点开；实况视频转发后为空时，可先播放原实况再重试。部分卡片类型不支持编辑转发。

准备媒体失败或超时会提示原因，不应把打开编辑器或开始准备当作发布成功。
