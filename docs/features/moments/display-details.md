# 底部详细信息

入口：WeKit 设置 → 功能 → 朋友圈 → 底部详细信息。

开启后，在朋友圈条目底部显示自定义详情。点击功能设置文本模板、时间格式，并选择是否隐藏可见范围图标。

可用占位符：`$originalText`、`$time`、`$type`、`$snsId`、`$userName`。默认模板是 `$time | $originalText`，默认时间格式为 `yyyy-MM-dd HH:mm:ss`。

需要为单条内容单独设置时，可同时启用[自定义底部详细信息](custom-details.md)。
