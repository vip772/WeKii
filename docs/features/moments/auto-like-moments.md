# 自动点赞

入口：WeKit 设置 → 功能 → 朋友圈 → 自动点赞。

按规则自动点赞或取消点赞。开启后点击功能，配置全局规则及联系人覆盖，未覆盖的项目继承全局设置。

可选择浏览时处理或处理所有已加载内容，并配置动作、操作间隔、时间范围、关键字、内容类型和发布时间范围。默认模式为浏览时处理、动作为点赞。

功能只处理微信已加载的内容，不会获得额外的朋友圈访问权限。动作会实际提交到微信，以返回结果为准。
