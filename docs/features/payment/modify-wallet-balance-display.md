# 修改显示余额

入口：WeKit 设置 → 功能 → 红包与支付 → 修改显示余额。

开启后点击功能，分别为零钱、零钱通和经营账户开启显示修改并设置余额表达式。

`value` 表示真实余额，例如 `value * 2`。支持设置页列出的算术运算、`sqrt` 和 `round`；默认各账户修改关闭，表达式为 `value`。

只改变本机余额文字，不改变实际账户资金，也不能作为付款或资产证明。
