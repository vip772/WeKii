# 分裂群组通话

入口：WeKit 设置 → 功能 → 娱乐 → 分裂群组通话。

启用后可从群成员/群组相关详情入口发起操作；点击功能也可选择目标。选择「发起假群通话并挂断」或「发起假群实时对讲机并终止」，填写大于 0 的重复次数并确认。

操作会生成临时假群 ID，发起并结束通话流程，结束后清理生成的假群。本功能会产生实际通话请求和对他人的提醒；仅应在参与者同意的测试中使用。已有任务运行时不能重复启动。
